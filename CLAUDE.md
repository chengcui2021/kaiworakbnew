# metamorphic-kb

Full-stack repo: `frontend/` (Vue 3 + TypeScript) and `backend/` (FastAPI + Python).

**For frontend work:** see [`frontend/CLAUDE.md`](frontend/CLAUDE.md) — Change Protocol, skills, hooks, and test coverage requirements live there.

Claude hooks (`.claude/`) and the pre-push git hook (`.githooks/pre-push`) are at the repo root and apply to all sessions.
