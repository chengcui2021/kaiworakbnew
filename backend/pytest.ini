[pytest]
# The backend already declares pytest under "# Dev / test" in requirements.txt;
# this file is what makes that declared harness actually runnable.
# Run from backend/:  pytest
testpaths = tests
pythonpath = .
addopts = -q
